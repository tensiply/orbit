name: Release

on:
  push:
    tags:
      # Stable ships only as vX.Y.0. Patch tags (vX.Y.Z, Z>=1) are canary
      # pre-releases handled by canary.yml and must not trigger a full release.
      - "v*.*.0"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. v0.5.0)"
        required: true

env:
  CARGO_TERM_COLOR: always

jobs:
  # ── Verify the tag matches the version in Cargo.toml ─────────────────────────
  verify:
    name: Verify version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.version }}
      tag: ${{ steps.ver.outputs.tag }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Extract version
        id: ver
        run: |
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          VERSION="${TAG#v}"
          # Enforce the policy: stable releases must be vX.Y.0.
          if [[ ! "${VERSION}" =~ ^[0-9]+\.[0-9]+\.0$ ]]; then
            echo "::error::Stable releases must be vX.Y.0 — got ${TAG}. Patch versions belong to the canary channel."
            exit 1
          fi
          echo "tag=${TAG}"     >> $GITHUB_OUTPUT
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Check Cargo.toml matches tag
        run: |
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          CARGO_VER=$(grep '^version' Cargo.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')
          if [ "v${CARGO_VER}" != "${TAG}" ]; then
            echo "::error::Version mismatch — Cargo.toml has v${CARGO_VER} but tag is ${TAG}"
            exit 1
          fi
          echo "Version verified: ${TAG}"

  # ── Build static binaries for all target platforms ────────────────────────────
  build:
    name: Build — ${{ matrix.artifact }}
    needs: verify
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            slug: linux-x86_64
            use_cross: true

          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            slug: linux-aarch64
            use_cross: true

          - os: macos-latest
            target: x86_64-apple-darwin
            slug: macos-x86_64
            use_cross: false

          - os: macos-latest
            target: aarch64-apple-darwin
            slug: macos-aarch64
            use_cross: false

    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross
        if: matrix.use_cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build (cross)
        if: matrix.use_cross
        run: cross build --release --target ${{ matrix.target }} --bin orbit

      - name: Build (native)
        if: "!matrix.use_cross"
        run: cargo build --release --target ${{ matrix.target }} --bin orbit

      - name: Name artifact
        run: |
          NAME=orbit-stable-${{ needs.verify.outputs.version }}-${{ matrix.slug }}
          cp target/${{ matrix.target }}/release/orbit "$NAME"
          chmod +x "$NAME"
          echo "ARTIFACT=$NAME" >> "$GITHUB_ENV"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARTIFACT }}
          path: ${{ env.ARTIFACT }}

  # ── Publish GitHub release ────────────────────────────────────────────────────
  release:
    name: Publish release
    needs: [verify, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create checksums
        run: |
          cd artifacts
          sha256sum orbit-* > checksums.txt
          cat checksums.txt

      - name: Extract release notes from CHANGELOG
        env:
          VERSION: ${{ needs.verify.outputs.version }}
        run: |
          python3 - <<'EOF'
          import re, os
          v = os.environ["VERSION"]
          with open("CHANGELOG.md") as f:
              cl = f.read()
          m = re.search(
              rf"(?m)^## \[{re.escape(v)}\][^\n]*\n(.*?)(?=^## \[|\Z)",
              cl, re.DOTALL
          )
          body = m.group(1).strip() if m else "_No changelog entry found._"
          with open("release_notes.md", "w") as f:
              f.write(body + "\n")
          EOF

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.verify.outputs.tag }}
          body_path: release_notes.md
          files: |
            artifacts/orbit-stable-*
            artifacts/checksums.txt

  # ── Update docs & wiki ────────────────────────────────────────────────────────
  docs:
    name: Update docs & wiki
    needs: [verify, release]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout main
        uses: actions/checkout@v7
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update CHANGELOG footer and README
        env:
          VERSION: ${{ needs.verify.outputs.version }}
          TAG: ${{ needs.verify.outputs.tag }}
          REPO: ${{ github.repository }}
        run: python3 .github/scripts/update_docs.py

      - name: Commit docs updates to main
        env:
          TAG: ${{ needs.verify.outputs.tag }}
        run: |
          git config user.name  "Eloir Corona"
          git config user.email "eloircoronah@outlook.com"
          git add CHANGELOG.md README.md
          git diff --cached --quiet || git commit -m "docs: update links for ${TAG}"
          git pull --rebase
          git push

      - name: Update wiki
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.verify.outputs.tag }}
          VERSION: ${{ needs.verify.outputs.version }}
          REPO: ${{ github.repository }}
        run: |
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${REPO}.wiki.git" wiki 2>/dev/null || {
            mkdir -p wiki
            git -C wiki init
            git -C wiki remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${REPO}.wiki.git"
          }

          cp release_notes.md "wiki/Release-${TAG}.md"
          python3 .github/scripts/build_wiki_home.py

          git -C wiki config user.name  "Eloir Corona"
          git -C wiki config user.email "eloircoronah@outlook.com"
          git -C wiki add .
          git -C wiki diff --cached --quiet || git -C wiki commit -m "Release ${TAG}"
          git -C wiki push --force -u origin HEAD:master
